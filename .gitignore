# Datos: NUNCA entran al repo. La ENMA se solicita por formulario a la fuente.
data/
*.csv
*.sav
*.dta
*.xls
*.xlsx

# Paquete de publicacion: el zip y el csv llevan la base, no se versionan.
# La licencia y el README del deposito son texto y si van al repo.
*.zip

# Utilitarios de preparacion de la base: quedan en local, no se publican.
# El repositorio contiene el material final, no las herramientas que lo produjeron.
scripts/

# Basura de RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Sistema
.DS_Store
