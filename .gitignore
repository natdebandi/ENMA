# Datos: NUNCA entran al repo. La ENMA se solicita por formulario a la fuente.
data/
*.csv
*.sav
*.dta
*.xls
*.xlsx

# Paquete del release: el zip y el csv llevan la base, no se versionan.
# La documentacion, la licencia y los pdf de la fuente son texto y si van al repo.
*.zip
paquete_release/

# Utilitarios de preparacion de la base: quedan en local, no se publican.
# El repositorio contiene el material final, no las herramientas que lo produjeron.
scripts/

# Basura de RStudio
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Sistema
.DS_Store
